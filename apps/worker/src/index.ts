/**
 * Worker stub. No job system, provider adapter, or pipeline in Phase 0.
 * Future home of WatchBot execution. Shared actions live in @openbento/domain.
 */

export function workerPlaceholder(): string {
  return "OpenBento worker stub — no job system in this foundation phase.";
}
