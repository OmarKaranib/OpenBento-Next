{
  "name": "worker",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "tsx src/index.ts --once",
    "start:loop": "tsx src/index.ts --loop",
    "start:fixture": "tsx src/index.ts --once --fixture"
  },
  "devDependencies": {
    "tsx": "^4.20.5"
  },
  "dependencies": {
    "@openbento/domain": "workspace:*",
    "@openbento/watchbot": "workspace:*"
  }
}
