{
  "name": "worker",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "tsx src/index.ts --once --fixture",
    "start:loop": "tsx src/index.ts --loop"
  },
  "devDependencies": {
    "tsx": "^4.20.5"
  },
  "dependencies": {
    "@openbento/domain": "workspace:*",
    "@openbento/watchbot": "workspace:*"
  }
}
